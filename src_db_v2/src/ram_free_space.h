// #ifndef RAM_FREE_SPACE_H
// #define RAM_FREE_SPACE_H

// #include <stddef.h>

// // Initialize RAM free space management system
// void ram_init_free_space();

// // Allocate memory from RAM using first-fit
// void *ram_allocate_memory(size_t size);

// // Free allocated RAM memory and merge adjacent blocks
// void ram_free_memory(void *ptr, size_t size);

// // Get current RAM free space statistics
// void ram_get_stats(size_t *total_free, size_t *largest_block, int *free_blocks);

// // Cleanup function to release RAM resources
// void ram_cleanup_free_space();

// #endif // RAM_FREE_SPACE_H
